#include <iostream>
#include <ctype.h>
#include <stdio.h>
#include <string>   //string
#include <fstream> //for ofstream class
using namespace std;

int main(){

  string x;
  ifstream fin("file.txt"); //Use for reading in rules
  getline(fin, x);
  fin.close();
return 0;
}
